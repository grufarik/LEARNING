#include <stdio.h>

/**
 * checking if 5 is greatwer than 10
 * If it is greater, "True" will be printed
 * If it is smaller, "False" will be printed
 * @i: the variable to be used
 *
 * Return: Always 0 when successful
 */

int main()
{
	int i=5;

	if (5>10)
		printf("True\n");
	else
		printf("False\n");
	return (0);
}
