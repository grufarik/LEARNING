#include <stdio.h>

/**
 * post increment - value changes after executionof code
 * i++: is used for post increment
 */

int main()
{
	int i=2;
	i++; //the value of I will be incresed by one

	printf("%d\n", i);
}
