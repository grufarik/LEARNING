#include <stdio.h>

/**
 * count numbers 1 to 10 followed by a new line.
 * Return: Always 0 when successful
 */
int main()
{
	int i;
	
	for (i = 1; i <= 10; i++)
	{
	printf("no. %d\n", i);
	}

	return (0);
}

