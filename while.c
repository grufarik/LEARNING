#include <stdio.h>

/**
 * Whille: the loop used to print "Terrific" seven times
 * followed by a new line
 * @i: the variable used.
 *
 * Return: Always 0 when successful
 */

int main ()
{
	int i=1;
	
	while (i<=7)
	{
		printf("Terrific\n");
		i++;
	}

	return (0);
}
